package HotelManagementSystem;

public class Payment {
    public boolean processPayment(double amount) {
        // Simulating payment process (return true for success)
        System.out.println("Processing payment of $" + amount);
        return true;
    }
}
